import UIKit //Para utilizar pow( )  e sqrt( ), potência e raiz quadrada, precisamos importar a classe UIKit.

var x, y, z, resultado: Double

x = 3
y = 2
z = 100

resultado = pow(x, y)
print("Resultado de elevar \(x) à \(y) é resultado \(resultado)")

resultado = sqrt(z)
print("Resultado da Raíz Quadrada de \(z) é \(resultado)")