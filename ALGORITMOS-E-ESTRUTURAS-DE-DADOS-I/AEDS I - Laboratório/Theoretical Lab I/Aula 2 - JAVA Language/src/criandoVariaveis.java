
public class criandoVariaveis {

	public static void main(String[] args) {
		// TODO Auto-generated method stub
		//Criando variáveis
        // Exemplos de vaiáveis a B x2 tempo a2b3 yz3

        //Declarando variáveis tipo int (Pertencente ao grupo de números inteiros)
        int A, B, x;

        //Declarando variável para tipo float(conjunto dos reais com ponto flutuante)
        float A2, b5, xy;

        //Declarando variável para tipo double(conjunto dos reais com ponto flutuante)
        double y, z, d4;

        //Se vamos armazenar caracteres, então declaramos variáveis do tipo char
        char letra= 'a';

        //Se vamos armazenar palavras, então declaramos variáveis do tipo String
        String palavra = "casa";

        //Para atribuir(falara que algo tem tal caracteristica) valores a variáveis usamos o sinal de = (igual)
        /*
            a=2;
            b=5;
            c= a + b;
        */
	}

}
